# tenants_api — Centralized API gateway for mobile / Flutter clients.
# Tenant resolution happens via JWT claim or X-Tenant-Slug header
# instead of subdomain, so mobile apps never need wildcard SSL.
